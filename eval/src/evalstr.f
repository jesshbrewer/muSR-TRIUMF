c      subroutine evalstr(in, out)
c      character*(*) in, out
c      
c      pos = 1 
c      if (in(1).eq.'$') then
c         ! extract name
c         do while (pos .le. nch .and. in(pos) .ne. ' ')
c         end do

c         ! check for assignment
c         if (in(pos) .eq. '=') return
c      end if
c      end
